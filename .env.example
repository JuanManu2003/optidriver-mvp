# Variables de entorno del FRONTEND (Vite).
# Copia a .env.local para desarrollo, o configúralas en Netlify para producción.

# Credenciales de Supabase (Supabase → Project Settings → API).
# Si las dejas vacías, la app funciona en modo local (simulador + localStorage).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
