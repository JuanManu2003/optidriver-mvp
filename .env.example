# Variables de entorno del FRONTEND (Vite).
# Copia a .env.local para desarrollo, o configúralas en Vercel para producción.

# URL del backend desplegado. DEJAR VACÍO en desarrollo (usa el proxy de Vite).
# En producción, apunta al backend en Render, p. ej.:
#   VITE_API_URL=https://optidriver-backend.onrender.com
VITE_API_URL=
