{
  "name": "optidriver-mvp",
  "private": true,
  "version": "1.0.0",
  "description": "OptiDriver / FuelSense Pro — MVP frontend para conductores en Chile",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:backend": "cd backend && npm run dev",
    "dev:all": "start \"backend\" cmd /k \"cd backend && npm run dev\" && vite",
    "build": "vite build",
    "preview": "vite preview",
    "backend:install": "cd backend && npm install"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
